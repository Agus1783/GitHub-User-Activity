# Github-User-Activity


A simple tool to track and display the public activity of a GitHub user. This project fetches data from the GitHub API to present relevant information in a readable format.

---

## About This Project

[GitHub User Activity] is a project I created for mini project backend dev from roadmap.sh to [explain the project's purpose, e.g., 'learn how to use the GitHub API', 'create a personal activity dashboard', or 'monitor team contributions'].

This project can fetch data such as recent commits, pull requests, issues, and new repositories created by a specific user.

### Features

* Displays a summary of user activity.
* Tracks public events (e.g., Commits, Pull Requests, Issues).
* [Add another main feature of your project].
* [Add another main feature...].

### Screenshot

(Add your application screenshots or demo GIF here. If you don't have one yet, you can state 'Demo coming soon.')

---

## Built With

The following are the main technologies and libraries used in this project:

* **[Programming Language]**: (e.g., Python, JavaScript, Go)
* **[Framework/Library]**: (e.g., React, Vue, Flask, Node.js, Express.js)
* **[API]**: (e.g., GitHub REST API v3 or GraphQL API v4)
* **[Other]**: (e.g., Bootstrap, Tailwind CSS for styling)

---

## Getting Started

To run this project on your local machine, follow these steps.

### Prerequisites

Ensure you have the following installed:
* [Prerequisite 1, e.g., Node.js v18+]
* [Prerequisite 2, e.g., Python v3.10+]
* [Prerequisite 3, e.g., GitHub API Token]

### Installation

1.  **Clone the repository:**
    ```sh
    git clone [https://github.com/Agus1783/GitHub-User-Activity.git](https://github.com/Agus1783/GitHub-User-Activity.git)
    ```

2.  **Navigate to the project directory:**
    ```sh
    cd GitHub-User-Activity
    ```

3.  **Install dependencies:**
    ```sh
    # Replace this command based on your package manager
    # Example for Node.js
    npm install
    # Example for Python
    pip install -r requirements.txt
    ```
---

## Usage

After installation, run the project with the following command:

```sh
# create venv
python3 -m venv venv # (linux / macOs)
python -m venv venv # (Windows)

# activate your venv
source venv/bin/activate # (linux / macOs)
venv/Scripts/activate # (Windows)

# install dependencies
pip3 install -r requirements.txt # linux / macOs 
pip install -r requirements.txt # Windows

# run project
python3 github_activity.py username # linux / macOs
python github_activity.py username # windows

# Correct me if i wrong, because i'm still learning
# project url
https://github.com/Agus1783/GitHub-User-Activity.git